import 'package:flutter/material.dart';

class ColorConfig {
  // Primary colors
  static const Color primaryColor = Color(0xFFdc3545);
  static const Color primaryLightColor = Color(0xFFdc3545);

  // Background colors
  static const Color backgroundColorLight = Colors.white;
  static const Color backgroundColorDark = Colors.black;
  static const Color scaffoldBackgroundLight = Color(0xFFF5F5F5);
  static const Color scaffoldBackgroundDark = Color(0xFF121212);

  // Text colors
  static const Color textPrimaryColor = Colors.white;
  static const Color textSecondaryColor = Colors.grey;
  static const Color hintTextColor = Colors.grey;

  // Button colors
  static const Color buttonBackground = Color(0xFFdc3545);
  static const Color buttonShadow = Color(0xFFdc3545);

  // Icon colors
  static const Color iconColorPrimary = Colors.grey;
  static const Color iconColorSecondary = Colors.grey;

  // Input field colors
  static const Color inputFillColor = Colors.grey;
  static const Color inputBorderColor = Colors.grey;

  // Gradient colors
  static const List<Color> primaryGradient = [
    Color(0xFFC82333),
    Color(0xFFDC3545),
    Color(0xFFFF5F6D),
  ];
}
